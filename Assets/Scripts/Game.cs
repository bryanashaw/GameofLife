using UnityEngine;

public class Game : MonoBehaviour
{
    [SerializeField] private int cellRows = 100;
    [SerializeField] private int cellColumns = 100;


    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
    }
}